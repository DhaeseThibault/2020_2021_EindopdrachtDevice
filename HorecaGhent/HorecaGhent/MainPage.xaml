<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="HorecaGhent.MainPage" NavigationPage.HasNavigationBar="False">

    <ContentPage.Content>
        <Grid RowSpacing="0" BackgroundColor="SteelBlue">
            <Grid.RowDefinitions>
                <RowDefinition Height="56"/>
                <RowDefinition Height="2"/>
                <RowDefinition Height="80"/>
            </Grid.RowDefinitions>

            <Grid Grid.Row="0">
                <BoxView BackgroundColor="LightSkyBlue"/>
                <Label Text="Horeca Ghent" FontSize="40" TextColor="White" FontAttributes="Bold" HorizontalOptions="Center" VerticalOptions="Center"/>
            </Grid>

            <Grid Grid.Row="1">
                <BoxView BackgroundColor="#696969"/>
            </Grid>

            <ListView x:Name="lvwRestaurants">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <ViewCell>
                            <Label Text="{Binding Name}" TextColor="Red"/>
                        </ViewCell>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </Grid>
       
    </ContentPage.Content>

</ContentPage>
